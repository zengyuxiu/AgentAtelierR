# 待机动作衔接修复（2026-09-20）

动作淡出后，Spine 会移除空轨道。直接在空轨道 setAnimation 并设置 mixDuration 没有混合来源，下一次动作可能突然进入。

- 新增 transitionCharacterTrack：仅对空轨道先放置空动画，再排入带混合时间的新动画；已有轨道直接交叉混合，不插入默认姿势。
- 动作组、单次反馈和休息姿势共用此入口，Android 与 Windows 一致。
- 盘腿的休息手位直接混合回原资源手位，避免先插入空动画。
- 单次反馈的淡出从片段结束时开始，不提前占用片段末尾。
- 不修改资源 alpha、速度、权重、动作幅度和选择频率。

验证：六套本地皮肤使用真实 Spine 原生运行库检查淡入、直接混合、淡出清理与参数保持；全套测试 366 通过、9 跳过，静态检查通过。自动化测试不替代真机视觉验收。

打包沿用 tool/build_protected.ps1 和现有签名配置，不上传原始皮肤、骨骼、贴图、音频、加密密钥。
